<%- include("partials/header")%> <%- include("partials/nav")%>
<div class="flex flex-col mt-5 px-10">
  <% if (message) { %>
  <p
    id="toast-message"
    class="bg-green-400 text-white font-semibold px-4 py-2 absolute rounded-full left-1/2 -translate-x-1/2"
    style="display: none; opacity: 0"
  >
    <%= message %>
  </p>
  <% } %>
  <h1 class="font-bold text-3xl">Profile page</h1>
  <a
    class="bg-red-500 hover:bg-red-600 transition-all duration-300 inline-block w-fit px-6 py-2 rounded-md font-semibold mt-3"
    href="/user/logout"
    id="logout-button"
    >Logout</a
  >
</div>
<%- include("partials/footer")%>

